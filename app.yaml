runtime: python311
instance_class: F2

env_variables:
  FLASK_ENV: "production"
  JWT_SECRET: "kisansetu-production-secret-sih-2026"

handlers:
  - url: /static
    static_dir: frontend
  - url: /.*
    script: auto
